Smiley
-----------
Setup:
Create your own "database/settings.php.inc" file (from "database/default.settings.php.inc") with database settings
Create your own "www/js/config.js" file (from "www/js/default.config.js") with serverlocation

The web client is located in www/

The SQL to setup the DB is here: "database/init_database.sql"
